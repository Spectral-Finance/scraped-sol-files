// SPDX-License-Identifier: MIT
pragma solidity ^0.8.6;

import "ds-test/test.sol";
import "./MerlinBitcoin.sol";

contract MerlinBitcoinTest is DSTest {
    MerlinBitcoin private _mb;

    function setUp() public {
        // _mb = new MerlinBitcoin(address(this));
    }

    function test_addAdmin() public {
      // _mb.addMinter(address(this));

      // address account = 0xC02aaA39b223FE8D0A0e5C4F27eAD9083C756Cc2;
      // assertEq(_mb.decimals(), 8);
      // uint256 balance = _mb.balanceOf(account);
      // assertEq(balance, 0);
      // uint256 amount = 999999;
      // _mb.mint(account, amount);
      // balance = _mb.balanceOf(account);
      // assertEq(balance, amount);
    }
}
